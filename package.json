{
    "name": "fp-workout-social",
    "version": "0.1.0",
    "description": "",
    "main": "server/server.js",
    "scripts": {
        "start": "node server/server.js",
        "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
        "build": "cd client && npm build",
        "start:dev": "concurrently \"cd server && node server\" \"cd client && npm start\""
    },
    "devDependencies": {
        "concurrently": "^8.0.1"
    }
}
